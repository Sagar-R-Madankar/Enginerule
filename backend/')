const express = require('express'); const bodyParser = require('body-parser'); const cors = require('cors'); const app = express(); app.use(bodyParser.json()); app.use(cors()); app.post('/evaluate-rule', (req, res) = const { rules, data } = req.body; const ast = generateAST(rules); const result = evaluateAST(ast, data); res.json({ ast, result }); }); function generateAST(rules) { let ast = []; for (let key in rules) { ast.push({ key: key, operator: rules[key].operator, value: rules[key].value, type: 'condition' }); } return ast; } function evaluateAST(ast, data) { let isValid = true; ast.forEach((rule) = const { key, operator, value } = rule; if (operator === ' { isValid = isValid 
